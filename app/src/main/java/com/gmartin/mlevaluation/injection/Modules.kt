package com.gmartin.mlevaluation.injection

import org.koin.android.ext.koin.androidContext
import org.koin.dsl.module

/**
 * Koin [module] to collect the definition of injectable classes.
 */
val applicationModule = module {
//    single(createdAtStart = true) { VehicleManagerProvider(androidContext()) }
//    single(createdAtStart = true) { USBManager(androidContext()) }
//    single(createdAtStart = true) { PlayerManager(androidContext()) }
//    single(createdAtStart = true) { FullScreenModeManager() }
//    single { ParkingBrakeManager() }
//    single { QueriesManager(androidContext()) }
//    single { PlaylistManager() }
}
